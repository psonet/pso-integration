build/
.gradle/
.kotlin

# Generated by uniffi-bindgen-mobile during the build-kotlin-jar CI job
src/main/kotlin/net/pso/zk/integration/mobile/pso_mobile_integration.kt

# Native dynamic libs staged into resources by the build-kotlin-jar
# CI job (3 cross-compiled libs: darwin-arm64 .dylib, linux-x86_64 .so,
# linux-aarch64 .so).
src/main/resources/META-INF/native/

# Native libs staged by the CI job before `gradle build` runs.
# Locally, point `-PnativeStageDir=...` at a directory outside the
# tree if you want to keep them around.
native/

# Bindgen binary staged by CI for `generateKotlinBindings`.
uniffi-bindgen-mobile
uniffi-bindgen-mobile-*
